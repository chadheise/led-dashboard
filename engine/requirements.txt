# Hardware canvas dependency (not on PyPI — install from repo root on the Pi):
#   git clone https://github.com/hzeller/rpi-rgb-led-matrix.git
#   <venv>/bin/pip3 install rpi-rgb-led-matrix/

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
watchfiles>=0.20.0
pyyaml>=6.0
Pillow>=10.0.0
httpx>=0.27.0
pydantic>=2.0.0
websockets>=12.0
timezonefinder>=6.0.0
cairosvg>=2.7.0
