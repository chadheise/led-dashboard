# Hardware canvas dependency (not on PyPI — must be built via Makefile on the Pi):
#   git clone https://github.com/hzeller/rpi-rgb-led-matrix.git
#   make -C rpi-rgb-led-matrix/bindings/python PYTHON=<venv>/bin/python3
#   make -C rpi-rgb-led-matrix/bindings/python install PYTHON=<venv>/bin/python3

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
watchfiles>=0.20.0
pyyaml>=6.0
Pillow>=10.0.0
httpx>=0.27.0
pydantic>=2.0.0
websockets>=12.0
